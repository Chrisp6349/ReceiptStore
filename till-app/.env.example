VITE_API_BASE_URL=http://localhost:4000/api

# Must match TILL_API_KEY in backend/.env. A real deployment would issue
# this per-device rather than baking it into the till's own build.
VITE_TILL_API_KEY=dev-till-key-change-me
